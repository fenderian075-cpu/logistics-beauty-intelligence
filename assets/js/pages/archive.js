import { el, link, byId, clear, root } from "../core/dom.js";
import { formatDate, formatMonth } from "../core/format.js";
import * as L from "../core/labels.js";
import { loadIntel } from "../data/intel.js";
import * as S from "../domain/signals.js";
import { bindLatestReportNav, markCurrent } from "../core/nav.js";
import { mountShell } from "../core/shell.js";
import { statusPill } from "../render/primitives.js";
import { filterBar, withinRange, monthsAgo } from "../render/filters.js";
let intel=null,bar=null;
function matches(report,state){if(state.type&&report.type!==state.type)return false;if(state.status&&report.status!==state.status)return false;if(!withinRange(report.date,state.from,state.to))return false;if(state.topic){const topic=intel.topic(state.topic),linked=topic&&(topic.related_report_ids||[]).indexOf(report.id)!==-1,viaSignal=S.signalsOf(report).some(sig=>sig.id===state.topic);if(!linked&&!viaSignal)return false;}if(state.lens||state.change||state.conf){const signals=S.signalsOf(report),match=signals.some(sig=>(!state.lens||sig.lens===state.lens)&&(!state.change||sig.change_status===state.change)&&(!state.conf||sig.confidence===state.conf));if(!match)return false;}if(state.q){const hay=[report.title,report.summary,report.bottom_line,report.tags.join(" "),report.date,report.type,report.key_issues.join(" "),S.signalsOf(report).map(s=>`${s.id} ${s.signal||""}`).join(" ")].join(" ").toLowerCase();if(!state.q.toLowerCase().split(/\s+/).every(term=>hay.indexOf(term)!==-1))return false;}return true;}
function entry(report,state){const li=el("li","archive-item");li.setAttribute("data-status",report.status);const meta=el("div","archive-item__meta");meta.appendChild(el("span","archive-item__date",report.type==="monthly"?formatMonth(report.period||report.date.slice(0,7)):formatDate(report.date)));meta.appendChild(el("span","archive-item__type",L.typeLabel(report.type)));li.appendChild(meta);const main=el("div"),head=el("div","archive-item__head"),h3=el("h3","archive-item__title");h3.appendChild(link(root()+report.path,null,report.title));head.appendChild(h3);head.appendChild(statusPill(report.status));const signals=S.signalsOf(report);head.appendChild(signals.length?el("span","archive-item__signals",`シグナル ${signals.length}`):el("span","archive-item__legacy",L.UI.archiveLegacy));main.appendChild(head);main.appendChild(el("p","archive-item__summary",report.summary));const structured=state.lens||state.change||state.conf,hits=structured?signals.filter(sig=>(!state.lens||sig.lens===state.lens)&&(!state.change||sig.change_status===state.change)&&(!state.conf||sig.confidence===state.conf)):[];if(hits.length){const box=el("ul","archive-item__hits");hits.slice(0,4).forEach(sig=>{const item=el("li");item.appendChild(el("span","archive-item__hit-lens",L.lensLabel(sig.lens)));item.appendChild(el("span","archive-item__hit-change",L.changeLabel(sig.change_status)));const topic=intel.topicForSignal(sig);item.appendChild(topic?link(`${root()}topic.html?id=${encodeURIComponent(topic.topic_id)}`,"chip-link",S.signalName(sig)):el("span",null,S.signalName(sig)));box.appendChild(item);});if(hits.length>4)box.appendChild(el("li","muted",`ほか ${hits.length-4} 件`));main.appendChild(box);}if(report.tags.length){const tags=el("div","tags");report.tags.forEach(tag=>tags.appendChild(link(`?q=${encodeURIComponent(tag)}`,"tag",tag)));main.appendChild(tags);}li.appendChild(main);return li;}
function render(state){const listEl=byId("archive-list"),emptyEl=byId("archive-empty"),hits=intel.reports.filter(r=>matches(r,state));clear(listEl);if(bar)bar.setCount(hits.length);if(emptyEl){emptyEl.hidden=hits.length>0;emptyEl.textContent=L.UI.archiveEmpty;}let currentYear=null;hits.forEach(report=>{const year=report.date.slice(0,4);if(year!==currentYear){currentYear=year;const heading=el("li","year-group",`${year}年`);heading.setAttribute("role","presentation");listEl.appendChild(heading);}listEl.appendChild(entry(report,state));});}
function presets(host){const rows=[["直近3か月で悪化",{from:monthsAgo(3),change:"deteriorating"}],["定時性の悪化",{lens:"reliability",change:"deteriorating"}],["新規シグナル",{change:"new"}],["高確度のみ",{conf:"high"}]],box=el("div","preset-row__inner");box.appendChild(el("span","preset-row__label","よく使う条件"));rows.forEach(([label,query])=>box.appendChild(link(`?${new URLSearchParams(query).toString()}`,"preset",label)));host.appendChild(box);}
export function init(){const listEl=byId("archive-list");if(!listEl)return Promise.resolve();return loadIntel().then(graph=>{intel=graph;bindLatestReportNav(intel.reports);markCurrent();mountShell(graph);const presetHost=byId("archive-presets");if(presetHost)presets(presetHost);const controls=byId("archive-filters");if(controls){bar=filterBar(controls,[{key:"type",label:"種別",type:"select",options:[["","すべて"],["daily","日次"],["weekly","週次"],["monthly","月次"]]},{key:"lens",label:"視点",type:"select",options:[["","すべて"],...S.LENSES.map(l=>[l,L.lensLabel(l)])]},{key:"change",label:"変化",type:"select",options:[["","すべて"],...S.CHANGE_PRIORITY.map(c=>[c,L.changeLabel(c)])]},{key:"conf",label:"確度",type:"select",options:[["","すべて"],["high","高"],["medium","中"],["low","低"]]},{key:"topic",label:"トピック",type:"select",options:[["","すべて"],...intel.topics.map(t=>[t.topic_id,t.title_ja||t.topic_id])]},{key:"status",label:"総合",type:"select",options:[["","すべて"],["normal","平常"],["watch","監視"],["disruption","障害"],["unconfirmed","未確認"]]},{key:"from",label:"開始日",type:"date"},{key:"to",label:"終了日",type:"date"},{key:"q",label:"キーワード",type:"search",placeholder:"タイトル・要約・シグナル"}],render);}render(bar?bar.state():{});}).catch(err=>{console.error(err);const emptyEl=byId("archive-empty");if(emptyEl){emptyEl.hidden=false;emptyEl.textContent=L.UI.loadError;}throw err;});}
